diff --git a/Makefile b/Makefile
index a3fe4eb..d447b4d 100644
--- a/Makefile
+++ b/Makefile
@@ -1,6 +1,6 @@
 SHELL=/bin/bash
 mlbuf_cflags:=$(CFLAGS) -D_GNU_SOURCE -Wall -g -fPIC
-mlbuf_ldlibs:=$(LDLIBS) -lpcre
+mlbuf_ldlibs:=$(LDLIBS) -L /usr/local/Cellar/pcre/8.36/lib -lpcre
 mlbuf_objects:=$(patsubst %.c,%.o,$(wildcard *.c))
 
 all: libmlbuf.so
